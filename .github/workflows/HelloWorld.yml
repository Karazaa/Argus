name: Hello World

on:
  workflow_dispatch:

jobs:
  update-runner: 
    runs-on: self-hosted
    steps:
      - name: Update Runner
        uses: Argus/UpdateRunner@latest

  hello-world:
    runs-on: self-hosted
    needs: 
      - update-runner
    steps:
      - name: Hello World!
        shell: powershell
        run: echo "Hello World!"
