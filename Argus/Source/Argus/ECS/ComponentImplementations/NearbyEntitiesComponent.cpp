// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#include "ComponentDefinitions/NearbyEntitiesComponent.h"
#include "ArgusEntity.h"
#include "ArgusLogging.h"
#include "ArgusMath.h"
#include "Serialization/Archive.h"

#if !UE_BUILD_SHIPPING
#include "ArgusStaticData.h"
#include "imgui.h"
#include "UObject/ReflectedTypeAccessors.h"
#endif //!UE_BUILD_SHIPPING

// Component shared functionality
uint16 NearbyEntitiesComponent::GetOwningEntityId() const
{
	return this - &ArgusComponentRegistry::s_NearbyEntitiesComponents[0];
}

void NearbyEntitiesComponent::Reset()
{
	m_nearbyEntities.ResetAll();
	m_nearbyFlyingEntities.ResetAll();
}

void NearbyEntitiesComponent::Serialize(FArchive& archive)
{
}

void NearbyEntitiesComponent::DrawComponentDebug() const
{
#if !UE_BUILD_SHIPPING
	if (!ImGui::CollapsingHeader("NearbyEntitiesComponent"))
	{
		return;
	}

	if (ImGui::BeginTable("ComponentValues", 2, ImGuiTableFlags_NoSavedSettings | ImGuiTableFlags_BordersInner))
	{
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyEntities.GetEntityIdsInAvoidanceRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyEntities.GetEntityIdsInAvoidanceRange().Max());
		if (m_nearbyEntities.GetEntityIdsInAvoidanceRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyEntities.GetEntityIdsInAvoidanceRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyEntities.GetEntityIdsInAvoidanceRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyEntities.GetEntityIdsInAvoidanceRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyEntities.GetEntityIdsInGroupExitRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyEntities.GetEntityIdsInGroupExitRange().Max());
		if (m_nearbyEntities.GetEntityIdsInGroupExitRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyEntities.GetEntityIdsInGroupExitRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyEntities.GetEntityIdsInGroupExitRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyEntities.GetEntityIdsInGroupExitRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyEntities.GetEntityIdsInSightRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyEntities.GetEntityIdsInSightRange().Max());
		if (m_nearbyEntities.GetEntityIdsInSightRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyEntities.GetEntityIdsInSightRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyEntities.GetEntityIdsInSightRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyEntities.GetEntityIdsInSightRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange().Max());
		if (m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyFlyingEntities.GetEntityIdsInAvoidanceRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange().Max());
		if (m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyFlyingEntities.GetEntityIdsInGroupExitRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_nearbyFlyingEntities.GetEntityIdsInSightRange()");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_nearbyFlyingEntities.GetEntityIdsInSightRange().Max());
		if (m_nearbyFlyingEntities.GetEntityIdsInSightRange().Num() == 0)
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_nearbyFlyingEntities.GetEntityIdsInSightRange().Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_nearbyFlyingEntities.GetEntityIdsInSightRange().Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				ImGui::Text("%d", m_nearbyFlyingEntities.GetEntityIdsInSightRange()[i]);
			}
			ImGui::Unindent();
		}
		ImGui::EndTable();
	}
#endif //!UE_BUILD_SHIPPING
}

// Per observable logic
