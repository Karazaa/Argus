// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#include "DynamicAllocComponentDefinitions/TeamCommanderResourceDataComponent.h"
#include "ArgusEntity.h"
#include "ArgusLogging.h"

#if !UE_BUILD_SHIPPING
#include "ArgusStaticData.h"
#include "imgui.h"
#include "UObject/ReflectedTypeAccessors.h"
#endif //!UE_BUILD_SHIPPING

// Component shared functionality
void TeamCommanderResourceDataComponent::Reset()
{
	m_seenResourceASourceExtractionData.Reset();
	m_seenResourceBSourceExtractionData.Reset();
	m_seenResourceCSourceExtractionData.Reset();
}

void TeamCommanderResourceDataComponent::Serialize(FArchive& archive)
{
	archive << m_seenResourceASourceExtractionData;
	archive << m_seenResourceBSourceExtractionData;
	archive << m_seenResourceCSourceExtractionData;
}

void TeamCommanderResourceDataComponent::DrawComponentDebug() const
{
#if !UE_BUILD_SHIPPING
	if (!ImGui::CollapsingHeader("TeamCommanderResourceDataComponent"))
	{
		return;
	}

	if (ImGui::BeginTable("ComponentValues", 2, ImGuiTableFlags_NoSavedSettings | ImGuiTableFlags_BordersInner))
	{
		ImGui::TableNextColumn();
		ImGui::Text("m_seenResourceASourceExtractionData");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_seenResourceASourceExtractionData.Max());
		if (m_seenResourceASourceExtractionData.IsEmpty())
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_seenResourceASourceExtractionData.Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_seenResourceASourceExtractionData.Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				m_seenResourceASourceExtractionData[i].DrawImGuiDebug();
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_seenResourceBSourceExtractionData");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_seenResourceBSourceExtractionData.Max());
		if (m_seenResourceBSourceExtractionData.IsEmpty())
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_seenResourceBSourceExtractionData.Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_seenResourceBSourceExtractionData.Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				m_seenResourceBSourceExtractionData[i].DrawImGuiDebug();
			}
			ImGui::Unindent();
		}
		ImGui::TableNextColumn();
		ImGui::Text("m_seenResourceCSourceExtractionData");
		ImGui::TableNextColumn();
		ImGui::Text("Array max is currently = %d", m_seenResourceCSourceExtractionData.Max());
		if (m_seenResourceCSourceExtractionData.IsEmpty())
		{
			ImGui::Text("Array is empty");
		}
		else
		{
			ImGui::Text("Size of array = %d", m_seenResourceCSourceExtractionData.Num());
			ImGui::Indent();
			for (int32 i = 0; i < m_seenResourceCSourceExtractionData.Num(); ++i)
			{
				if (i != 0) ImGui::Separator();
				m_seenResourceCSourceExtractionData[i].DrawImGuiDebug();
			}
			ImGui::Unindent();
		}
		ImGui::EndTable();
	}
#endif //!UE_BUILD_SHIPPING
}
