// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#pragma once

#include "ArgusContainerAllocator.h"
#include "CoreMinimal.h"
#include "ComponentDependencies/TaskComponentStates.h"

class IFogOfWarLocationComponentObserver
{
public:
};

class FogOfWarLocationComponentObservers
{
private:
	TArray<IFogOfWarLocationComponentObserver*, ArgusContainerAllocator<2> > m_FogOfWarLocationComponentObservers;

public:
	void AddObserver(IFogOfWarLocationComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FogOfWarLocationComponentObservers.Add(observer);
	}

	void RemoveObserver(IFogOfWarLocationComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FogOfWarLocationComponentObservers.RemoveAll
		(
			[observer] (IFogOfWarLocationComponentObserver* other)
			{
				return other == observer;
			}
		);
	}

	void Reset()
	{
		m_FogOfWarLocationComponentObservers.Reset();
	}

private:

	friend struct FogOfWarLocationComponent;
};
