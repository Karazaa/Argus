// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#pragma once

#include "ArgusContainerAllocator.h"
#include "CoreMinimal.h"
#include "ComponentDependencies/TaskComponentStates.h"

class IFacingComponentObserver
{
public:
};

class FacingComponentObservers
{
private:
	TArray<IFacingComponentObserver*, ArgusContainerAllocator<2u> > m_FacingComponentObservers;

public:
	void AddObserver(IFacingComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FacingComponentObservers.Add(observer);
	}

	void RemoveObserver(IFacingComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FacingComponentObservers.RemoveAll
		(
			[observer] (IFacingComponentObserver* other)
			{
				return other == observer;
			}
		);
	}

	void Reset()
	{
		m_FacingComponentObservers.Reset();
	}

private:

	friend struct FacingComponent;
};
