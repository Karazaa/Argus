// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#pragma once

#include "ArgusContainerAllocator.h"
#include "CoreMinimal.h"
#include "ComponentDependencies/TaskComponentStates.h"

class INearbyObstaclesComponentObserver
{
public:
};

class NearbyObstaclesComponentObservers
{
private:
	TArray<INearbyObstaclesComponentObserver*, ArgusContainerAllocator<2u> > m_NearbyObstaclesComponentObservers;

public:
	void AddObserver(INearbyObstaclesComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_NearbyObstaclesComponentObservers.Add(observer);
	}

	void RemoveObserver(INearbyObstaclesComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_NearbyObstaclesComponentObservers.RemoveAll
		(
			[observer] (INearbyObstaclesComponentObserver* other)
			{
				return other == observer;
			}
		);
	}

	void Reset()
	{
		m_NearbyObstaclesComponentObservers.Reset();
	}

private:

	friend struct NearbyObstaclesComponent;
};
