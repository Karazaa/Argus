// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#pragma once

#include "ArgusContainerAllocator.h"
#include "CoreMinimal.h"
#include "ComponentDependencies/TaskComponentStates.h"

class IFlockingComponentObserver
{
public:
};

class FlockingComponentObservers
{
private:
	TArray<IFlockingComponentObserver*, ArgusContainerAllocator<2u> > m_FlockingComponentObservers;

public:
	void AddObserver(IFlockingComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FlockingComponentObservers.Add(observer);
	}

	void RemoveObserver(IFlockingComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_FlockingComponentObservers.RemoveAll
		(
			[observer] (IFlockingComponentObserver* other)
			{
				return other == observer;
			}
		);
	}

	void Reset()
	{
		m_FlockingComponentObservers.Reset();
	}

private:

	friend struct FlockingComponent;
};
