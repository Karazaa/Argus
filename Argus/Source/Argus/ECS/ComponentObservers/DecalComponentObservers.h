// Copyright Karazaa. This is a part of an RTS project called Argus.
// AUTOGENERATED FILE

#pragma once

#include "ArgusContainerAllocator.h"
#include "CoreMinimal.h"
#include "ComponentDependencies/TaskComponentStates.h"

class IDecalComponentObserver
{
public:
};

class DecalComponentObservers
{
private:
	TArray<IDecalComponentObserver*, ArgusContainerAllocator<2> > m_DecalComponentObservers;

public:
	void AddObserver(IDecalComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_DecalComponentObservers.Add(observer);
	}

	void RemoveObserver(IDecalComponentObserver* observer)
	{
		if (!observer)
		{
			return;
		}

		m_DecalComponentObservers.RemoveAll
		(
			[observer] (IDecalComponentObserver* other)
			{
				return other == observer;
			}
		);
	}

	void Reset()
	{
		m_DecalComponentObservers.Reset();
	}

private:

	friend struct DecalComponent;
};
