#pragma region #####
public:
	const #####* Get#####(uint32 id);
#if WITH_EDITOR
	const uint32 Add#####ToDatabase(#####* record);
#endif

protected:
	UPROPERTY(EditAnywhere)
	TSoftObjectPtr<#####Database> m_#####Database;
	UPROPERTY(Transient)
	TObjectPtr<#####Database> m_#####DatabasePersistent;
#pragma endregion