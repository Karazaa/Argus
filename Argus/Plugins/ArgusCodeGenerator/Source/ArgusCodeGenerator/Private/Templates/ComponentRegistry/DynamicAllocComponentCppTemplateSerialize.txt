	numComponents = s_#####s.Num();
	archive << numComponents;
	if (archive.IsLoading())
	{
		for (int32 i = 0; i < numComponents; ++i)
		{
			uint16 entityId = 0;
			archive << entityId;

			#####* component = GetOrAddComponent<#####>(entityId);
			if (component)
			{
				component->Serialize(archive);
			}
		}
	}
	else
	{
		for (TPair<uint16, #####*>& pair : s_#####s)
		{
			archive << pair.Key;
			if (pair.Value)
			{
				pair.Value->Serialize(archive);
			}
		}
	}