#pragma region #####
	const size_t #####Size = sizeof(#####);
	netSize += #####Size;
	ARGUS_LOG(ArgusTestingLog, Display, TEXT("[%s] Size of %s = %d"), ARGUS_FUNCNAME, ARGUS_NAMEOF(#####), #####Size);
#pragma endregion