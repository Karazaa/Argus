	s_#####s.RemoveAll([](const uint16& entityId, #####*& component)
		{
			if (ArgusEntity::IsReservedEntityId(entityId) && component)
			{
				component->Reset();
				return false;
			}

			return true;
		}
	);
 